class Player {
    constructor() {
        this.coins = 100;
        this.apple = 0;
        this.limit = 0;  // 期限
    }

var value = Math.floor( Math.random() * (max + 1 - min) ) + min;

const player = new Player();
